{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "pwa-msedge",
            "request": "launch",
            "name": "Launch Program",
            "url": "http://localhost:3007",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}"
        }
    ]
}